/*
1. print the size of all the data types in c++.
2. print all the data types using typeid().name() function.
3. input a number and print it.
4. get two integer numbers as input from the user and design a calculator.
5. get three float numbers as input find the product and print it.
6. take two string from the user as input print them in one line with a space in between.
*/

#include<bits\stdc++.h>
using namespace std;

int main(){

    // program 1

    // cout<<sizeof(string)<<endl;
    // cout<<sizeof(int)<<endl;
    // cout<<sizeof(float)<<endl;
    // cout<<sizeof(double)<<endl;
    // cout<<sizeof(char)<<endl;
    // cout<<sizeof(bool)<<endl;

    // program 2

    // float f= 8.9;
    // cout<<typeid(4).name()<<endl;
    // cout<<typeid(f).name()<<endl;
    // cout<<typeid(4.893457632465389146589346589365983469853245).name()<<endl;
    // cout<<typeid('c').name()<<endl;
    // cout<<typeid(true).name()<<endl;

    // program 3

    // int number;
    // cout<<"enter a number"<<endl;
    // cin>>number;
    // cout<<number;

    // program 4

    // int n1,n2;
    // cout<<"enter the two numbers"<<endl;
    // cin>>n1>>n2;
    // cout<<"addition : "<<(n1+n2)<<endl;
    // cout<<"substraction : "<<(n1-n2)<<endl;
    // cout<<"multiplication : "<<(n1*n2)<<endl;
    // cout<<"division : "<<(n1/n2)<<endl;
    // cout<<"quotient : "<<(n1%n2)<<endl;

    // program 5

    // float f1,f2,f3;
    // cout<<"enter three float numbers"<<endl;
    // cin>>f1>>f2>>f3;
    // cout<<fixed<<setprecision(2)<<"the product = "<<(f1*f2*f3)<<endl;
    // cout<<"the product = "<<(f1*f2*f3)<<endl;

    // program 6

    // string str1,str2;
    // cout<<"enter two string"<<endl;
    // getline(cin,str1);
    // getline(cin,str2);
    // cout<<str1<<" "<<str2<<endl;

    // cout<<boolalpha;
    // cout<<(100>50);
    // cout<<(100<50);

    /*
    operators in C++
    1. Arithematic operator
      i) +  (addition)
      ii) -  (substraction)
      iii) *  (multiplication)
      iv) /   (division)
      v) %  (modulus)

    2. Relational operator
        i) >   (greater than)
        ii) <  (less than)
        iii) >=   (greater than or equal to)
        iv) <=   (less than or equal to)
        v) ==   (equal to)
        vi) !=   (not equal to)

    3. Assignment operator
        i) =
        ii) +=
        iii) *=
        iv) -=
        v) /=
        vi) %=

    4. logical operator
        i) &&  (and)
        ii) ||  (or)
        iii) !  (not)

    5. Bitwise operator
        i) &  (bitwise and)
        ii) |  (bitwise or)
        iii) ~  (bitwise not)
        iv) << (left right)
        v) >> (right right)
        vi) ^ (xor)
    */
}